<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl"
          crossorigin="anonymous">
    <title>Sign up</title>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark flex-column">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">AKSP5 VASILEV</a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
            <form class="d-flex">
                <a href="/login" role="button" class="btn btn-outline-light">Sign in</a>
            </form>
        </div>
    </div>
</nav>
<div class="container pt-5 d-flex justify-content-center">
    <form name="signUpUser" method="post" action="/sign-up">
        <div class="mb-3">
            <label for="usernameInput" class="form-label">Username</label>
            <input class="form-control {{#alreadyExists}}is-invalid{{/alreadyExists}}"
                   id="usernameInput"
                   name="username" type="text" placeholder="username">
            <div class="invalid-feedback">
                User already exists
            </div>
        </div>
        <div class="mb-3">
            <label for="passwordInput" class="form-label">Password</label>
            <input class="form-control {{#notMatched}}is-invalid{{/notMatched}}"
                   id="passwordInput"
                   name="password" type="password"
                   placeholder="••••••••">
            <div class="invalid-feedback">
                Password does not match
            </div>
        </div>
        <div class="mb-3">
            <label for="matchingPasswordInput" class="form-label">Confirm
                password</label>
            <input class="form-control {{#notMatched}}is-invalid{{/notMatched}}"
                   id="matchingPasswordInput"
                   name="matchingPassword" type="password"
                   placeholder="••••••••">
            <div class="invalid-feedback">
                Password does not match
            </div>
        </div>
        <button type="submit" class="btn btn-outline-primary">Sign up</button>
    </form>
</div>
</body>
</html>
© 2021 GitHub, Inc.
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl"
          crossorigin="anonymous">
    <title>Home page</title>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">AKSP FIVE PRACTICE</a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page"
                       href="/home">Home page</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/university/add">Add
                        university</a>
                </li>
            </ul>
            <form class="d-flex">
                <a href="/logout" role="button" class="btn btn-outline-light">Sign out</a>
            </form>
        </div>
    </div>
</nav>
<div class="container pt-4">
    <div class="row">
        {{#universities}}
            <div class="col-sm-6 pt-4">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center"
                             role="university">
                            <h2 class="card-title">{{universityName}}</h2>
                            <div class="d-flex justify-content-end align-items-center"
                                 role="university">
                                <form method="post" action="/university/{{id}}"
                                      class="px-2">
                                    <button type="submit"
                                            class="btn btn-outline-primary btn-sm">
                                        Open
                                    </button>
                                </form>
                                <form method="post"
                                      action="/university/{{id}}/delete">
                                    <button type="submit"
                                            class="btn btn-outline-danger btn-sm">
                                        Delete
                                    </button>
                                </form>
                            </div>
                        </div>
                        <ul class="list-university pt-4">
                            {{#students}}
                                <form method="post"
                                      action="/student/{{id}}/delete"
                                      class="pt-2">
                                    <li class="list-university-item d-flex justify-content-between align-items-center">
                                        {{lastName}} {{firstName}} {{middleName}}
                                        <button type="submit"
                                                class="btn btn-outline-danger btn-sm">
                                            Remove
                                        </button>
                                    </li>
                                </form>
                            {{/students}}
                        </ul>
                    </div>
                </div>
            </div>
        {{/universities}}
    </div>
    {{^universities}}
        <h3>No universities added...</h3>
    {{/universities}}
</div>
</body>
</html>
